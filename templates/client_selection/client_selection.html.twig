{% extends 'base.html.twig' %}

{% block title %}Sélection de Client{% endblock %}

{% block body %}
    <h1>Sélectionnez une Operation</h1>

    {{ form_start(form) }}
    <div class="form-group">
        {{ form_label(form.operation, 'Operation') }}
        {{ form_widget(form.operation, {'attr': {'class': 'form-control'}}) }}
        {{ form_errors(form.operation) }}
    </div>
    <a href="{{ path('app_email' }}"><button type="submit" class="btn btn-primary">Valider</button></a>
    {{ form_end(form) }}

    {# Affichez des messages flash ou d'autres informations ici #}
{% endblock %}



