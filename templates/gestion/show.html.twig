{% extends 'base.html.twig' %}

{% block title %}Employe{% endblock %}

{% block body %}
    <h1>Employe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ employe.id }}</td>
            </tr>
            <tr>
                <th>Matricule</th>
                <td>{{ employe.matricule }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ employe.roles ? employe.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ employe.password }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ employe.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ employe.prenom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_gestion_index') }}">back to list</a>

    <a href="{{ path('app_gestion_edit', {'id': employe.id}) }}">edit</a>

    {{ include('gestion/_delete_form.html.twig') }}
{% endblock %}
